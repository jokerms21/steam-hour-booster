services:
  booster:
    container_name: booster
    build: .
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json
      - ./steam-data:/app/steam-data
      - ./tokens:/app/tokens
    stdin_open: true
    tty: true
